import React, { Component } from 'react'
import styles from './styles.css';

export default class App extends Component {
    render() {
        return (
            <div className={styles['app-wrapper']}>
                A boilerplate for electron-react app.
            </div>
        )
    }
}
 